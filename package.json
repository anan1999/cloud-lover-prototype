{
  "name": "cloud-lover-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Lover AI companion prototype with multi-provider fallback.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js",
    "generate:eval-bank": "node scripts/generate-evaluation-bank.js",
    "smoke:quality": "node scripts/run-quality-smoke.js"
  },
  "dependencies": {
    "pg": "^8.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
